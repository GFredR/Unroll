# Xcode
DerivedData/
.build_data/
xcuserdata/
*.xcuserstate
*.xcscmblueprint

# xcodegen 生成物:project.yml 才是唯一真源,改完配置跑 Scripts/regen.sh
*.xcodeproj/
Unroll/Info.plist
# QuickLook 两个 appex 的 Info.plist 同样是 project.yml 里 info: 段生成的
# (内容是未展开的 $(PRODUCT_MODULE_NAME),由 Xcode 在构建期展开)
UnrollQuickLook/*/Info.plist

# SwiftPM
.build/
.swiftpm/

# macOS
.DS_Store

# 临时
*.orig
*.rej

# 发布前检用的额外泄漏关键词(真实姓名等)。**只放本机,绝不入库** ——
# 这个文件的存在本身就是为了让真实身份不进仓库
Scripts/.identity-patterns

# 手工验收样本与工具编译产物(2026-09-11:样本由 Scripts/gen_big_sample.swift 现造)
TestSamples/
Scripts/gen_big_sample
Scripts/bench_archive

# 讲解类文档(个人学习产出,按 2026-08-26 定下的规则不入库)
# 与 ARCHITECTURE / 测试文档这类「技术文档」不同 —— 它是「讲这个项目怎么想出来的」,
# 面向自己回看,不是给仓库读者读的。文件留在本机,只是不进版本控制。
docs/教学-*.html

# 内部工程笔记(2026-09-24 定).这两份是**运营手册**性质,不是给仓库读者的:
#   测试与验证.md —— 267 KB 单体内部台账(含泄漏扫描三通道的做法)
#   发布清单.md   —— 内部发布 SOP(含 gh 建仓命令与笔名隔离策略的说明)
# 它们不泄漏真实身份(三通道扫过),但会公开「作者在刻意隔离身份」这件事本身。
# ⚠️ 只加进 .gitignore 是**不够**的 —— 它们在 40 / 22 笔历史里出现过,
#    必须连历史一起抹掉,否则 `git show <旧提交>:<路径>` 照样能读全文。
#    抹历史的命令与理由见 Scripts/publish.sh 的注释段。
# ⚠️ 副作用(会咬人):**遵守 .gitignore 的搜索工具从此看不见这两份文件**
#    (ripgrep / 本机 Grep 工具的默认行为)。做「文档同步」时它们恰恰是必须改的 —— 
#    得用 `--no-ignore` 或直接给绝对路径,否则会以为「仓库里没有这两份文档」。
docs/测试与验证.md
docs/发布清单.md
